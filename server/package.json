{
  "name": "@minidrama/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc --noEmit",
    "migrate": "tsx src/db/cli.ts up",
    "migrate:down": "tsx src/db/cli.ts down",
    "check:migrate": "tsx src/db/check-migrate-cli.ts",
    "check:integration": "tsx src/db/check-integration-cli.ts"
  },
  "dependencies": {
    "@minidrama/config": "workspace:*",
    "@minidrama/shared": "workspace:*",
    "fastify": "^5.6.1"
  },
  "devDependencies": {
    "@types/node": "^22.18.9",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
