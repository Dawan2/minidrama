# W16 — G1.9 leftover: require a requirement/defect id

> **Slot:** W16, work slot (`bc-89fef1d3`). One backlog item, no pull request.
> **Branch:** `cursor/w16-work-g19-72c4`, cut from `origin/main` at **`614f656`**.
> Merged forward onto **`e59b124`** (`bc-72e30448` landed the G1.9 *format* job at
> `cf7ecd4`; double-tap favourite also landed).
> **Item:** **G1.9** remainder / **D-20** — the 需求/缺陷编号 half. Conventional Commits
> format was already a required L1 step. `feat: add a widget` is still red.
> **Not in scope:** D-17 billing. C4-03 Postgres. C4-07 VIP. C5-01 G1.10 (on main).
> C5-02 / D-18 (on main). C5-03 / D-19 (P3's file). Rewriting history on `main`.
> A second `check:commits` job. No pull request.

---

## 1. What was picked, and why

At dispatch, C5-01 G1.10 was on `main` (`614f656`) and a sibling said **G1.9 stays open**.
This slot implemented the full gate. While that ran, `bc-72e30448` landed the format-only
half (`cf7ecd4` / `docs/handoff/w16-c5-more.md`). Their reverse fixture `feat: add skip
detection` exits 0. The gate text is "Conventional Commits;**PR 必须关联需求/缺陷编号**".
That second clause was still missing. This slot keeps their job and adds the id.

| Item | State at pick / after merge |
| --- | --- |
| D-17 / C4-03 / C4-07 | Skipped |
| C5-01 G1.10 / G1.7 | On `main`. Not retaken |
| C5-02 / D-18 | On `main`. Not a second route |
| G1.9 format (`bc-72e30448`) | **Landed** `cf7ecd4`. Kept. Not a second job |
| **G1.9 需求/缺陷编号** | **This slot** |
| C5-03 / D-19 | P3's file |

A comment that names Conventional Commits is not G1.9. Reverse verification for this
remainder is a fixture whose unique commit is `feat: add skip detection`.

---

## 2. What changed

`pnpm run check:commits` still uses merge-base with `origin/main` through `HEAD` and
`--no-merges`. The log format is now `%s%x00%b%x1e` so a footer `Refs: D-20` counts. A
prose subject is still red. A Conventional header with no tracker id is now red. An empty
range is still 0 new commits, not a skip.

| File | Change |
| --- | --- |
| `packages/quality/src/commits.ts` | Tracker policy on the landed job |
| `packages/quality/src/cli/check-commits.test.ts` | `feat: add skip detection` is missing-id red |
| `docs/14-quality-gates.md` §2.1 | Dated note: the id half is the job |

`.github/workflows/l2.yml` is unchanged. `docs/plan/cycle-5-backlog.md` is not rewritten.
Wallet top-up stays disabled. `adUnlock` stays false.

---

## 3. Reverse verification

Against a fixture whose unique commit is `feat: add skip detection`:

```
commits failed (1): new commits that are not Conventional Commits with a requirement/defect id are G1.9 red
  missing-id feat: add skip detection
```

Exit 1. Against `feat(g1.9): add skip detection`: exit 0. Against `feat: add skip
detection` with footer `Refs: D-20`: exit 0. Against `Wire the skip detector`: `prose`
(unchanged). History on `main` is not rewritten.

---

## 4. Overlap with in-flight

| Who | Overlap |
| --- | --- |
| `bc-72e30448` G1.9 format | **Idle. Landed** `cf7ecd4`. This slot does not replace the job |
| `bc-8416a6dd` leftover C5 | **Idle.** Double-tap favourite on `main`. Player files not edited here |
| `bc-264077b7` next C5 after like-gesture | **RUNNING.** Not this slice |
| C4-03 / C4-07 / D-17 | Unchanged |

---

## 5. Verification

`pnpm verify` numbers are recorded after the suite on this branch (post-merge).

---

## 6. Left open

- **D-17.** Billing. Cannot be closed from a branch.
- **C5-03 / D-19.** P3 writeback of `wave-protocol.md` §6.2.
- **QA-011 / QA-010** a11y. Do not add axe-core as a drive-by.
- **PLY-012** token re-issue.
- **C4-03 / C4-07.** Do not fake; no subscription contract.
- **`docs/plan/cycle-5-backlog.md`.** Not rewritten.
