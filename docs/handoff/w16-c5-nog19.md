# W16 — QA-011: a11y is a release blocker (C-12 writeback)

> **Slot:** W16, work slot (`bc-17fae197`). One backlog item, no pull request.
> **Branch:** `cursor/w16-work-c5-nog19-72c4`, cut from `origin/main` at **`e59b124`**
> (G1.9 `pnpm check:commits` already on main).
> **Item:** `QA-011` / **C-12** / **X-12** — adopt `docs/plan/definition-of-done.md` §6:
> a11y is a listing blocker. Same-batch writebacks **X-04** (`14-test-plan` §6.2 TikTok
> WebView matrix) and **X-05** (`14-quality-gates` §5.4 Minis ZIP / JS-error budgets).
> **Not in scope:** D-17 billing. C4-03 Postgres. C4-07 VIP. G1.9 (`e59b124`; sibling
> `bc-89fef1d3` should no-op). In-flight after-like `bc-264077b7` (PLY-012 token
> re-issue). `QA-010` axe-core job. PNL-05 / `playbackRate` (X-26). C5-03 / D-19 P3
> writeback of `wave-protocol.md` §6.2. No pull request.

---

## 1. What was picked, and why

`docs/plan/cycle-5-backlog.md` remaining unblocked engineering after skipping D-17,
C4-03, and C4-07, and after items already on `main` or in flight:

| Item | State at pick |
| --- | --- |
| D-17 GitHub Actions billing | Rank 1. **Not a branch.** Skipped |
| G1.9 Conventional Commits | **On `main`** at `e59b124`. Not retaken. Sibling `bc-89fef1d3` left to no-op |
| C5-01 / D-20 G1.10 | **On `main`**. G1.7 dated as G2.5 Trivy |
| C5-02 / D-18 wallet transactions | **On `main`** |
| Double-tap 点赞 (PLY-010 remainder) | **On `main`** (`d297708`) |
| PLY-012 token re-issue | **RUNNING** (`bc-264077b7` after-like). Left |
| C4-03 T14/T16/T15 | Do not fake. Skipped |
| C4-07 SCR-11 / D8 | No contract. Skipped |
| C5-03 / D-19 `wave-protocol.md` §6.2 | P3's file |
| 倍速 / axe-core as leftover | Forbidden by the C5 plan rank-2 close |
| **QA-011 / C-12 a11y adjudication** | **This slot.** Named W17 remainder; writeback, not the axe-core job |

C5 ranked `QA-011 then QA-010`. `QA-010` is protocol-C4 exit 3 (axe-core in CI) and is
not opened here. `QA-011` is the conflict writeback slot C owns: `docs/14-test-plan.md`
§6.4 still said "P2, 不阻断首个上架版本" while DoD §6 already raised the bar.

---

## 2. What changed

**Adopt**, not weaken. `14-test-plan` §6.4 now matches DoD §6. P1 countersign is still
owed on `docs/plan/backlog.md` (P1's file; not rewritten). P3's DoD is not rewritten:
`VF-12` (S-A1/S-A8 scan a deleted PNL-05) does not hold on this tree — PNL-05 is in
`docs/02-screen-inventory.md` again.

| File | Change |
| --- | --- |
| `docs/14-test-plan.md` §6.4 | Heading and body: 上架阻断. Points at DoD S-A1…S-A10. Records that `acceptance-criteria.md` has no a11y group. Names `QA-010` as the later CI job, not this slice |
| `docs/14-test-plan.md` §6.2 | TikTok iOS WKWebView × Android Chromium WebView. WeChat / Safari / Chrome are not the listing host (`X-04`) |
| `docs/14-quality-gates.md` §2.1 | Dated note for the three writebacks |
| `docs/14-quality-gates.md` §5.3 / G3.8 / §5.4 | Minis ZIP ≤ 20 MB, first-screen JS ≤ 300 KB gzip, JS error < 0.5% / blank < 0.1%. Native APK/ANR N/A (`X-05`) |
| `packages/quality/src/markdown-section.ts` | Heading slice used by the reverse-verification tests |
| `packages/quality/src/qa-011-adjudication.test.ts` | Committed docs must keep the adopted wording |

`docs/plan/cycle-5-backlog.md` is not rewritten. `docs/plan/definition-of-done.md` and
`docs/plan/wave-protocol.md` are not rewritten. Wallet top-up stays disabled.
`adUnlock` stays false. No axe-core job. No BytePlus `vid`.

---

## 3. Reverse verification

Against a test plan whose §6.4 heading is restored to `P2,不阻断首个上架版本`:

```
FAIL  is present as a release blocker rather than a P2 observation
```

Against a §6.2 heading restored to `云真机` with `**H5**:微信内置 WebView`:

```
FAIL  names TikTok WebView as the host and drops WeChat as a matrix row
```

Against §5.4 rows restored to `安卓包体积(APK/AAB 下载大小)`:

```
FAIL  replaces native APK/iOS rows with ZIP and first-screen JS
```

Exit 1. A missing heading returns an empty section and fails `not.toBe('')`. Wiring
axe-core is still `QA-010`; this slice asserts the writeback *says so*.

---

## 4. In-flight overlap

| Who | Overlap |
| --- | --- |
| `bc-89fef1d3` G1.9 leftover | **RUNNING.** Quality `commits.ts` / `ci.yml`. Untouched |
| `bc-264077b7` after-like | **RUNNING.** Expected pick: PLY-012 play-token re-issue. Player / playback-api files untouched |
| `bc-72e30448` G1.9 remainder | **Idle. Landed** `e59b124`. Not retaken |
| `bc-8416a6dd` double-tap | **Idle. Landed.** Player files not edited |
| Playback UX siblings | **Idle. Landed.** Swipe / autoplay / cross-end / drama-detail Continue kept |

`git diff origin/main -- app/ server/ .github/` is empty of this slot's work.

---

## 5. Verify

Recorded after `pnpm verify` on this branch.

---

## 6. What is still open

- **PLY-012** token re-issue. In flight (`bc-264077b7`). Still no play-token refresh path on `main` at pick.
- **QA-010** a11y gate. Adjudication landed; axe-core job is the next C item and is **not** this leftover.
- **P1 countersign** of C-12 on `docs/plan/backlog.md`. Slot C does not rewrite P1's file.
- **D-17.** Billing. Local verify is not CI.
- **C4-03 / C4-07**, GATE-7 / GATE-8, Beans. Unchanged.
- **C5-03 / D-19.** P3 writeback of `wave-protocol.md` §6.2.
- **`docs/plan/cycle-5-backlog.md`.** Not rewritten. The document belongs to the plan slot.

This slice does **not** claim protocol-C4 exit 3 closed. It is the named `QA-011` writeback.
