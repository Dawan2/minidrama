# W18 — X-26 remainder: VePlayer-owned scrub (progress plugin + seek inference)

> **Slot:** W18, work slot (`bc-c68b4e10`). One protocol-C4 sheet remainder, no pull request.
> **Branch:** `cursor/w18-work-x26-72c4`, cut from `origin/main` at **`68d2747`**
> (C6 plan). Fast-forwarded onto **`3e8bdb2`** (QA-010 axe-core landed while this slot wrote).
> **Item:** **scrub** — `01-product-scope` §4.3 进度条拖拽与预览. VePlayer constructor
> `ignores` keeps the progress plugin (`sitemap-and-ia.md` §4.1). A `timeupdate`
> discontinuity flushes progress (`player-state-machine.md` §4.3). Horizontal drag is
> not 切集. No competing bar, no native `<video>`, no BytePlus `vid`.
> **Not in scope:** D-17 billing, C4-03 Postgres, C4-07 VIP, QA-010 (`bc-b0108787`,
> landed at `3e8bdb2`), 倍速 / `playbackRate` / 0.75 (X-26; sibling did not take it —
> they took stall chrome on `cursor/w18-work-interaction-72c4`), `#/vip`. No pull request.

---

## 1. What was picked, and why

`docs/plan/cycle-6-backlog.md` ranks D-17 first (not code) and protocol-C4 交互验收单
second. Remaining named sheet halves at pick: **倍速** and **scrub**. Sibling
`bc-402f89a0` (`cursor/w18-work-interaction-72c4`) implemented stall indicator / retry
(`player-stall.ts`), not either control. This slot takes **scrub**.

| Item | State at pick |
| --- | --- |
| D-17 GitHub Actions billing | Rank 1. **Not a branch.** Skipped |
| QA-010 axe-core | **RUNNING** at pick (`bc-b0108787`). **Landed** `3e8bdb2` while this slot wrote. Not retaken |
| 倍速 / client `playbackRate` | X-26 unadjudicated. Sibling did not take it. **Left.** `playbackrate` stays in `ignores`. Do not pick 0.75 |
| C4-03 / C4-07 | Do not fake / no contract. Skipped |
| Sibling stall chrome | `cursor/w18-work-interaction-72c4`. Not an ancestor of this tip. Overlaps `PlayerSurface.tsx` / `app.css` if composed later |
| **PLY-010 scrub** | **This slot.** Plugin-owned progress bar + seek inference. VePlayer APIs only |

PNL-05 stays deleted. AC-PL-6: the app renders no competing progress control.

---

## 2. What changed

Constructor `ignores` matches the official immersive sample, **minus** any progress
plugin name — scrubbing is kept by omission. `closeVideoClick: false` /
`closeVideoDblclick: true` so tap-pause stays VePlayer's and double-tap 点赞 stays
ours.

A `timeupdate` that jumps backward more than 1 s, or forward more than 1.5 × wall
clock, is a scrub: flush the new position, do not wait for the heartbeat interval,
do not emit a second play. A predominantly horizontal drag is not 切集; the surface
does not `preventDefault` it.

| File | Change |
| --- | --- |
| `app/src/player/veplayer-plugins.ts` | `VEPLAYER_IGNORED_PLUGINS`; progress kept by omission |
| `app/src/player/veplayer-types.ts` | `ignores`, `closeVideoClick`, `closeVideoDblclick` |
| `app/src/player/player-facade.ts` | Forwards the plugin policy |
| `app/src/player/seek-inference.ts` | Discontinuity predicate |
| `app/src/player/progress-heartbeat.ts` | Flush on inferred scrub |
| `app/src/player/episode-swipe.ts` | `isHorizontalScrub` |
| `app/src/player/PlayerSurface.tsx` | Horizontal drag is not 切集 |
| `app/src/player/mock-veplayer.ts` | `data-veplayer-progress="kept"`; `scrub()` is a `timeupdate` |
| `app/src/styles/app.css` | Comment: progress plugin stays VePlayer's |

No `playbackRate` constant. No `<input type="range">`. No `<video>`. No `vid_demo_`.

---

## 3. Mutations that bite

Run against this branch, then reverted.

### 3.1 `ignores` lists a progress plugin

```
FAIL  keeps the progress plugin and does not invent a playbackRate ladder
expected [ … 'progress' ] not to match /progress/i
```

### 3.2 Horizontal drag still fires 切集

```
FAIL  does not treat a horizontal scrub as 切集, so the progress plugin keeps the drag
AssertionError: expected "spy" to not be called at all, but actually been called 1 times
```

### 3.3 A 5 → 40 jump waits for the 10 s interval

```
FAIL  flushes a plugin scrub immediately and does not wait for the interval
expected [] to deeply equal [ 40 ]
```

---

## 4. In-flight compose

| Who | Overlap |
| --- | --- |
| `bc-b0108787` QA-010 | **Landed** `3e8bdb2`. Quality package / `ci.yml`. This slot fast-forwarded. Did not retake axe files |
| `bc-402f89a0` remaining sheet | **RUNNING.** Stall chrome on `PlayerSurface.tsx` / `PlayPage.tsx` / `app.css`. Not 倍速, not scrub. Compose if it lands before merge |

G2.3 stays the L2 `smoke` job. D-17 is still billing. No BytePlus ingest ids.

---

## 5. Verify

`pnpm verify` after absorbing `origin/main` (`3e8bdb2`). Numbers in the following
commit / this file's postscript.

Native `<video>` remains absent. Rate plugin stays ignored (X-26).

---

## 6. What is still open

- **倍速.** `playbackrate` is still in `ignores`. Do not invent 0.75. Sibling did not
  take this control.
- **Full 交互验收单.** Stall chrome is on the sibling branch, not this tip. Tap pause
  stays VePlayer-owned.
- **D-17** GitHub Actions billing. Local verify is not CI.
- **C4-03 / C4-07**, GATE-7 / GATE-8, Beans. Unchanged.
- **Sibling stall compose** if `cursor/w18-work-interaction-72c4` is not an ancestor
  of `main` at merge time.

This slice does **not** claim protocol-C4 exit 1 closed. It is the named remaining
scrub half of the sheet. 倍速 stays X-26.
