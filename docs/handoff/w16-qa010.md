# W16 — QA-010: smallest axe-core a11y scan

> **Slot:** W16, work slot (`bc-b0108787`). One backlog item, no pull request.
> **Branch:** `cursor/w16-work-qa010-72c4`, cut from `origin/main` at **`7ce8620`**
> (QA-011 a11y adjudication already on main). Merged forward onto **`68d2747`**
> (PLY-012 and the C6 plan landed while this slot ran).
> **Item:** `QA-010` — smallest a11y scan that does not skip tests and does not
> claim TikTok WebView. axe-core critical + serious = 0 on the committed SCR-13
> fixture; an injected contrast violation is red.
> **Not in scope:** D-17 billing. C4-03 Postgres. C4-07 VIP. PLY-012 (`bc-264077b7`,
> landed `aa8a9e8` while this slot ran). Cycle-5 verify docs (`bc-1ebe19d3`).
> C5-03 / D-19 P3 writeback of `wave-protocol.md` §6.2. PNL-05 / `playbackRate`
> (X-26). Protocol-C4 exit 3 / S-A1 on every SCR/PNL. No pull request.

---

## 1. What was picked, and why

`docs/plan/cycle-5-backlog.md` ranked `QA-011 then QA-010`. QA-011 landed on
`main` at `7ce8620`. That handoff named the axe-core job as the next C item.

| Item | State at pick |
| --- | --- |
| D-17 GitHub Actions billing | Rank 1. **Not a branch.** Skipped |
| C4-03 T14/T16/T15 | Do not fake. Skipped |
| C4-07 SCR-11 / D8 | No contract. Skipped |
| PLY-012 token re-issue | **RUNNING** (`bc-264077b7`). Left. Landed later at `aa8a9e8` |
| Cycle-5 verify docs | **RUNNING** (`bc-1ebe19d3`). Left. Landed as `docs/verify/cycle-5-report.md` |
| C5-01 / D-20, C5-02 / D-18, G1.9, QA-011 | **On `main`** |
| **QA-010 axe-core scan** | **This slot.** Unblocked by QA-011. Not already owned |

This is the smallest scan: one implemented screen (SCR-13 fallback), real
axe-core, no skipped tests, host labelled jsdom **not** TikTok WebView.

---

## 2. What changed

`pnpm run check:a11y` runs axe-core in jsdom over
`packages/quality/a11y/screens/*.html`. Missing screens, a missing required
`scr-13-fallback` stem, a missing axe-core/jsdom engine, critical/serious
violations, or declared CSS contrast below 4.5:1 exit 1. There is no
`continue-on-error`. Tests are not skipped. `color-contrast` is not disabled;
jsdom cannot *complete* that rule (no canvas), so the equivalent checker on
declared CSS colors is the S-A2 reverse path.

| File | Change |
| --- | --- |
| `packages/quality/src/a11y.ts` | Scan + axe-core + contrast policy |
| `packages/quality/src/cli/check-a11y.ts` | Process boundary |
| `packages/quality/a11y/screens/scr-13-fallback.html` | SCR-13 fixture (`lang="en"`, product body colors) |
| `.github/workflows/ci.yml` | Step `QA-010 a11y` after G1.10 |
| `package.json` | `check:a11y` in `verify` |
| `packages/quality/src/licenses.ts` | Named G2.8 case: `axe-core` MPL-2.0 only. Other MPL still red |
| `docs/14-quality-gates.md` §2.1, `docs/14-test-plan.md` §6.4 | Dated notes. QA-011 phrasing kept |

`docs/plan/cycle-5-backlog.md` and `docs/plan/cycle-6-backlog.md` are not
rewritten. Wallet top-up stays disabled. `adUnlock` stays false. No BytePlus
`vid`. Player files were not edited here.

---

## 3. Reverse verification

Against a fixture whose unique HTML is `p { color: #ffffff; background: #ffffff; }`:

```
a11y failed (1): axe-core critical/serious or contrast < 4.5:1 are QA-010 red (host=jsdom, not TikTok WebView)
  contrast … color-contrast p 1.00:1 < 4.5:1
```

Exit 1. Against a fixture whose `<html>` has no `lang`:

```
a11y failed (1): … html-has-lang …
```

Exit 1. That second fixture is how this slice proves axe-core ran, not only
the contrast checker. Against a source directory with no `*.html`:

```
scan source has no HTML screens: an a11y scan that saw no screens has not run
```

Exit 1. A comment that names WCAG is not this gate. Stdout on green says
`host=jsdom, not TikTok WebView`.

---

## 4. In-flight overlap

| Who | Overlap |
| --- | --- |
| `bc-264077b7` PLY-012 | **Idle. Landed** `aa8a9e8` / `cursor/w16-work-c5-after-like-72c4` while this slot merged. Player files not edited here |
| `bc-1ebe19d3` cycle-5 verify | **Idle. Landed** `docs/verify/cycle-5-report.md`. Not rewritten |
| `bc-4233e1b3` W18 C6 plan | **Idle. Landed** `docs/plan/cycle-6-backlog.md` at `68d2747`. Rank 3 named this agent in flight. Plan file not edited here |

`git diff origin/main -- app/ server/` is empty of this slot's work.

---

## 5. Verify

`pnpm verify` exited 0 on this branch after merging `origin/main` (`68d2747`,
PLY-012 + C6 plan).

| Gate | Result |
|---|---|
| Format / lint / types | pass |
| G1.9 commits | `2 new commits vs origin/main, 0 prose, 0 missing-id` (merge commits skipped) |
| G1.10 skips | `231 test files, 0 skips, 0 empty` |
| QA-010 a11y | `a11y passed (1 screens, 0 critical, 0 serious, host=jsdom, not TikTok WebView)` |
| Tests + coverage | **3,480 passing** — shared 63, quality 391, config 45, server 1,785, app 1,196. Coverage: global lines 94.25% (17178/18226), branches 90.97%, core 95.70%, **diff lines 98.17% (321/327)** |
| Build | pass — `index-CUut56eC.js` 360.61 kB / 110.26 kB gzip (PLY-012's client; this slot did not edit product UI) |
| Guardrails | `platform guardrails passed (artifact: /workspace/app/dist)` |

Native `<video>` remains absent. Test count did not fall. jsdom prints
`HTMLCanvasElement's getContext()` while axe attempts `color-contrast`; that
is incomplete, not a skip, and the equivalent contrast checker is the reverse
path.

---

## 6. What is still open

- **S-A1 on every SCR/PNL.** This slice scans SCR-13 only. Remaining implemented
  screens are a later QA-010 remainder.
- **S-A2 on live `app.css`.** `--accent` (#fe2c55) under white label text is
  below 4.5:1. Not remediated here; the fixture uses passing body colors.
- **TikTok WebView.** Not claimed. PLY-002 still `unmeasured`.
- **Protocol-C4 exit 3.** Not closed. This is the named `QA-010` smallest job.
- **D-17.** Billing. Local verify is not CI.
- **C4-03 / C4-07**, GATE-7 / GATE-8, Beans. Unchanged.
- **C5-03 / D-19.** P3 writeback of `wave-protocol.md` §6.2.
- **`docs/plan/cycle-5-backlog.md` / `cycle-6-backlog.md`.** Plan-slot files.

This slice does **not** claim protocol-C4 exit 3 closed. It is the named
`QA-010` smallest axe-core scan.
